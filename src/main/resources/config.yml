prefix: ""

bookshelf:
  enable: false
  endpoint: https://bookshelf.mineverse.io:3000
  socket: ws://bookshelf.mineverse.io:7688
  token: 'your bookshelf token'

item-command:
  enable: false
  name: "Item"
  lore: []
  slot: 17
  material: PAPER

  # already integrate with PlaceholderAPI
  command: help 

  # custom mode data work with texture pack
  custom-data: 0

  # possible value is: player, console, operator
  run-as: player
